import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  iconContainer: {
    position: 'absolute',
    bottom: 0,
    right: 0,
    width: 30,
    height: 30,
  },
  icon: {
    width: 30,
    height: 30,
  },
})

export default styles
